{
  "name": "petmii",
  "version": "1.0.0",
  "description": "A desktop pet companion app",
  "main": "out/main/main.js",
  "scripts": {
    "dev": "electron-vite dev -- --disable-gpu --no-sandbox",
    "dev:clean": "rm -rf out node_modules/.vite && electron-vite build && electron-vite dev -- --disable-gpu --no-sandbox",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "electron-builder"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0",
    "electron-vite": "^2.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "@testing-library/react": "^14.1.0",
    "@testing-library/jest-dom": "^6.1.0",
    "jsdom": "^23.0.0",
    "fast-check": "^3.15.0"
  }
}
